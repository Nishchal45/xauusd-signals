services:
  - type: web
    name: xauusd-signals
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
```

**3. Add `.python-version`** — create a new file called exactly `.python-version` (with the dot) containing just:
```
3.11.9
